spring:
  application:
   name: portfolio
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/portfolio
    username: postgres
    password: postgres
    